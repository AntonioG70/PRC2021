extends layout
block content
    .w3-card-4
        header.w3-container.w3-teal
            h3 Lista de Periodos
            a(href='http://localhost:7777/') Voltar à página inicial

        if info.length > 0
            .w3-container
                table(class="w3-table-all")
                    tr 
                      th Nome
                    each p in info
                      tr
                        td
                          a(href='http://localhost:7777/periodos/' + p.p) #{p.p}
        else
          div(class="w3-container")
            p Periodos não encontrados ... 
        footer(class="w3-container w3-teal")
          address Gerado por António :: 2021-Março    