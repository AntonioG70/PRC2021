extends layout
block content
    .w3-card-4
        header.w3-container.w3-teal
            h3 Lista de Indivíduos da Classe #{idClasse}
            a(href='http://localhost:7777/repositorios/' + idREP) Voltar atrás 

        if info.length > 0
            .w3-container
                table(class="w3-table-all")
                    tr 
                      th Nome
                    each i in info
                      tr
                        td
                          a(href='http://localhost:7777/repositorios/' + idREP + '/' + idClasse + '/' + i.i) #{i.i}
        else
          div(class="w3-container")
            p Ainda não há individuos resgistados na classe ... 
        footer(class="w3-container w3-teal")
            address Gerado por António :: 2021-Março    